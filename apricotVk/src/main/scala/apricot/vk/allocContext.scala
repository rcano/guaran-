package apricot.vk

/* Provides integration with apricot.util.directmem package
 * There's an implicit AllocContext whenever there's an implicit lwjgl MemoryStack, and provide a class that does the same over malloc
 */

